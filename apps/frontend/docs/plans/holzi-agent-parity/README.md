# Holzi Agent Parity Roadmap

This folder contains session-sized implementation plans for growing Holzi into
the central personal agent hub:

- one always-on agent core on the server
- many clients: Web UI, Signal, Telegram, Cline/Roo, later haex-vault and MCPs
- shared memory, tools, conversations, credentials, and operational controls

The goal is not to clone `nesquena/hermes-webui` line by line. The goal is to
bring the same class of capabilities into Holzi in a way that fits Holzi's
backend-first architecture.

## Architecture Principle

Holzi is the agent core.

`holzi-frontend`, Signal, Telegram, Cline/Roo, and future integrations are
clients. New capabilities should live in the backend whenever they affect agent
state, memory, tools, conversations, approvals, workspaces, or model routing.
The frontend should make those capabilities visible and pleasant to operate.

**Holzi konfigurierbar via Holzi.** Every persistent configuration —
personas, channels, workspaces, LLM credentials, messenger accounts, tools,
MCP servers, skills — must be reachable from `/settings/*` in the Control
Center. No piece of Holzi's runtime behaviour should require editing files
on the host or restarting a container to change. The agent itself should
ultimately be able to inspect and (with the user's approval) provision the
same surfaces via meta-tools, so the question „has Holzi got service X?"
has the same answer whether the user clicks through the UI or asks the
agent in chat. Plans that add a new runtime knob carry a Control-Center
slot with them.

## Naming

- **Holzi** is the name of the agent and of the whole system as observed from
  the outside.
- The backend repository historically uses the package name `hermes`
  (`src/hermes/...`). New code should keep that import path until a coordinated
  rename happens; user-facing strings always say "Holzi".

## Deployment Model

- One agent runs per container, single worker, single user. The agent
  container is the always-on, lightweight surface.
- Multi-user scaling is achieved by running more agent containers, not more
  workers per container.
- Plans that rely on in-memory state (cancel registry, approval futures,
  SSE buffers) assume this invariant. Plan 03b documents it explicitly.
- Any operation that runs code, executes shell, or performs unbounded file
  writes lives in a sandbox container, not the agent. See Plan 11b for
  topology (workspace sandboxes + ephemeral execution sandboxes). The agent
  itself stays unkillable: "unkillable" means the core agent loop is never
  killed by user code or tool calls — sandbox crashes are isolated by
  design. **MCP servers (Plan 32) are an exception worth knowing:** they
  run in-process inside the agent container, not in a sandbox. A
  pathological MCP server can therefore exhaust agent resources (memory,
  fds, CPU). Mitigations are user-side: only install trusted servers,
  `mcp_install` requires approval (Plan 21 / 32-A), and Plan 32's
  health-watcher surfaces crashes. Future isolation for MCP servers is a
  separate plan, not part of the 31–33 family.

## Status

Completed:

- [01](./01-conversation-lifecycle.md) — conversation lifecycle (2026-05-25)
- [01b](./01b-conversation-retention-and-bookmarks.md) — retention + bookmarks (2026-05-26)
- [02](./02-conversation-search-organization.md) — search and organization (2026-05-26; follow-up [Holzi#33](https://github.com/haexhub/Holzi/pull/33) merged)
- [03](./03-chat-cancel-and-run-state.md) — chat cancel and run state (2026-05-26)
- [03b](./03b-agent-runs-and-observability.md) — agent runs and observability (2026-05-26)
- [04](./04-chat-retry-last-response.md) — chat retry last response (2026-05-26)
- [05](./05-chat-edit-and-regenerate.md) — chat edit and regenerate (2026-05-27)
- [06](./06-streaming-resilience-and-queueing.md) — streaming resilience and queued sends (2026-05-27; frontend-only [holzi-frontend#29](https://github.com/haexhub/holzi-frontend/pull/29) merged)
- [07](./07-chat-rendering-polish.md) — chat rendering polish (2026-05-27; frontend-only [holzi-frontend#30](https://github.com/haexhub/holzi-frontend/pull/30) merged)
- [08](./08-tool-call-cards.md) — tool call cards + event taxonomy (2026-05-27; cross-repo [Holzi#38](https://github.com/haexhub/Holzi/pull/38) + [holzi-frontend#31](https://github.com/haexhub/holzi-frontend/pull/31) merged)
- [09](./09-approval-cards.md) — approvals and safety cards (2026-05-27; cross-repo [Holzi#39](https://github.com/haexhub/Holzi/pull/39) + [holzi-frontend#32](https://github.com/haexhub/holzi-frontend/pull/32) merged)
- [10](./10-reasoning-and-subagent-cards.md) — reasoning and subagent cards (2026-05-27; cross-repo [Holzi#40](https://github.com/haexhub/Holzi/pull/40) + [holzi-frontend#34](https://github.com/haexhub/holzi-frontend/pull/34) merged). Reasoning end-to-end; subagent events are the wire contract + cards only (no orchestrator yet).
- [11](./11-attachments.md) — attachments (2026-05-27; cross-repo [Holzi#42](https://github.com/haexhub/Holzi/pull/42) + [holzi-frontend#36](https://github.com/haexhub/holzi-frontend/pull/36) merged). Text/code/log inlined into agent context; images + PDF stored as metadata-only (no provider image inputs yet). Added `POST /api/conversations` so the first message of a fresh chat can carry attachments.
- [11b-a](./11b-a-sandbox-spine.md) — sandbox spine: lifecycle + exec + limits + network isolation (2026-05-28; cross-repo [Holzi#43](https://github.com/haexhub/Holzi/pull/43) + [holzi-frontend#38](https://github.com/haexhub/holzi-frontend/pull/38) merged). Rootless Podman, `SandboxManager` + `PodmanSandboxBackend`, sandboxes run with `NetworkMode none` so the isolation criterion holds against real Podman. Verified live: 3/3 integration tests green (exec demux, kill resilience, network unreachability). Host prerequisites (subuid/subgid, cpu cgroup delegation, podman ≥4.x) encoded in the ansible `podman_debian` role.
- [11b-b](./11b-sandbox-runtime.md) — sandbox runtime remainder: file API + health watcher + `sandbox_crashed` event + restart endpoint + in-chat crash card (2026-05-28; cross-repo [Holzi#44](https://github.com/haexhub/Holzi/pull/44) + [holzi-frontend#40](https://github.com/haexhub/holzi-frontend/pull/40) merged). `read_file`/`write_file` on the `SandboxBackend` Protocol (Podman uses `/containers/{id}/archive` tar-wrapping with streaming + 10 MiB cap); `git` stayed as plain `exec(["git", ...])`. `SandboxManager` health watcher (surface-only, never auto-restarts) plus dedupe by (workspace_id, sandbox_id). `sandbox_crashed` SSE event in the existing envelope; the chat stream subscribes a per-request crash handler. `GET /api/workspaces/{id}/sandbox` (returns `absent` when no handle is cached) and `POST .../restart`. Frontend `SandboxCrashCard.vue` is rendered in `pages/index.vue` as a conversation-scoped banner; **Workspace badge → Plan 12, persistent crash log → Plan 20**.
- [12](./12-workspace-browser-readonly.md) — read-only workspace browser (2026-05-29; cross-repo [Holzi#45](https://github.com/haexhub/Holzi/pull/45) + [holzi-frontend#42](https://github.com/haexhub/holzi-frontend/pull/42) merged). New `list_dir` on `SandboxBackend` (Podman uses `find -mindepth 1 -maxdepth 1 -printf …` with an `sh -c` guard mapping missing/not-a-dir to exit codes 44/45). Three endpoints under `/api/workspace/{roots,tree,file}` route through Plan 11b-b's sandbox-mounted `/workspace` volume — never the host filesystem; `HERMES_WORKSPACE_ROOTS` configures the visible workspace_ids. Preview classification: text (256 KiB cap with truncation flag), markdown, image (2 MiB base64 cap, extension-only, no magic-bytes), binary metadata-only; SVG previews as XML source not as an opaque image. `SandboxNotRunning` surfaces as 503 on both routes so the existing `sandbox_crashed` UI applies. Frontend `WorkspacePanel.vue` mounted as the 4th right-panel tab with breadcrumb, dir listing, refresh, stale-response guard, and `aria-live` preview region.
- [13](./13-workspace-write-git.md) — workspace write + git status (2026-05-29; cross-repo [Holzi#46](https://github.com/haexhub/Holzi/pull/46) + [holzi-frontend#44](https://github.com/haexhub/holzi-frontend/pull/44) merged). `POST/PUT/DELETE /api/workspace/file`, `POST /api/workspace/rename`, and `GET /api/workspace/git` — all mutations route through the workspace sandbox (`write_file` + `exec(["mv"...])` / `exec(["rm"...])`) and are followed by `git add -A` + `git -c user.name/email=Holzi commit -m "user[conv-N]: <action> <path>"` when the root is a git repo; non-repo roots return `committed: false` and the write still goes through. `GET /file` now returns the on-disk `sha256` so writers pass it back as `base_sha`; mismatch → 409 (Conflict Card UI deferred to bind-mount mode). NUL bytes in `content` → 400 (binary-as-text guard). `_stat_entry` normalises missing parent / "not a directory" parents into 404/400 instead of raw `SandboxError`; `_git_commit` swallows `SandboxError` so a sandbox crash mid-commit can't 500 after a successful write. Frontend `WorkspacePanel.vue` grows edit mode (textarea + Save with `base_sha` round-trip, neutral `saveNotice` for non-git success vs destructive `saveError`), `Neue Datei` inline create form, rename + delete behind `window.prompt`/`confirm`, branch + dirty/clean badge. Writes are gated by an active chat (`conversationId`) so every commit message has an honest author tag; losing the conversation mid-edit drops the draft. `FakeSandboxBackend.script_exec` became a FIFO queue and gained `recorded_execs` so tests can assert the git commit argv.
- [14](./14-control-center-shell.md) — Control Center shell (2026-05-29; frontend-only [holzi-frontend#46](https://github.com/haexhub/holzi-frontend/pull/46) merged). `/settings` reworked into a Control Center with a single nav model in `app/lib/settingsNav.ts` consumed by both the desktop sidebar (sticky, 208 px wide) and mobile top-tabs in `pages/settings.vue`. Six placeholder routes (`preferences`, `memory`, `tasks`, `skills`, `workspaces`, `diagnostics`) all render the shared `components/settings/PlaceholderSection.vue` which looks itself up in the nav model by `route.path`. Shipped sections (`llm`, `messenger`) keep their existing content untouched; placeholders carry an `upcoming` hint and an empty-state card with no fake functionality. `/settings` still redirects to `/settings/llm`. Future plans (15 memory, 16 tasks, 20 diagnostics) just drop the `upcoming` hint and add real content.
- [15](./15-memory-panel.md) — memory panel (2026-05-29; cross-repo [Holzi#48](https://github.com/haexhub/Holzi/pull/48) + [holzi-frontend#48](https://github.com/haexhub/holzi-frontend/pull/48) merged). Adds optional `?q=` to `GET /api/notes` backed by the existing FTS5 helper with a small `_fts5_query` sanitiser (whitespace-split, alnum-only tokens, quoted phrases) so the search box never leaks FTS5 operator syntax; whitespace-only `q` falls through to `list_all` (review fix, matches CodeRabbit's flag). Replaces the `/settings/memory` placeholder with a **Hermes WebUI-style two-pane layout** — left sidebar with "Personal memory" header + `+` button + search + note list (key, markdown preview, tag chips, selected-row highlight); right detail view flips between empty / read (full markdown via `RenderedMarkdown`) / edit modes with Edit/Trash and Cancel/Save header buttons mirroring Hermes WebUI exactly. Stale-selection-after-filter guard (both inside `load()` and `cancelEdit()`) keeps the detail pane in sync with the visible list. Verified live via `make up-local-full` + Playwright. Right-side rail in `pages/index.vue` lost the **Todos + Reminders tabs** (per user request "die todos, reminder bar kann gerne weg") — only Notes + Workspace remain; `TodosPanel.vue` + `RemindersPanel.vue` and their type aliases are deleted, backend endpoints stay.
- [16](./16-tasks-cron-panel.md) — tasks and cron panel (2026-05-29; cross-repo [Holzi#49](https://github.com/haexhub/Holzi/pull/49) + [holzi-frontend#51](https://github.com/haexhub/holzi-frontend/pull/51) merged). **Scope deviation from the plan file**: after confirming with the user that `reminders` + `todos` had no UI left after Plan 15 and only existed as agent tools, both were dropped entirely in favour of a single `agent_tasks` concept. New `agent_tasks` table carries one-shot (`due_at`) and recurring (5-field cron + IANA `timezone`) jobs; recurring rows persist both `schedule` (rule) and a materialised `due_at` (cached next firing) so the scheduler's hot-path query stays index-friendly. `AgentTaskScheduler` replaces `ReminderScheduler`, fires due rows via fresh `channel="task"` conversations through `run_agent` with `track_run(agent_task_id=...)`; tool calls inside route through `SandboxManager` exactly like `/api/chat`. `mark_run(advance=True/False)` advances cron / disables one-shots when called by the scheduler, leaves them untouched when called from `POST /api/tasks/{id}/run` (the manual run-now path). PATCH cleanly enforces "switching recurring→one-shot needs explicit `due_at`" so the cached cron timestamp can't silently become the one-shot. Frontend `/settings/tasks` mirrors the Plan 15 two-pane shape; runs default tz to `Intl.DateTimeFormat().resolvedOptions().timeZone`; run-now schedules two delayed `load()` polls (1.5s + 4s, cancelled on unmount) so the user sees the agent's actual outcome land. Agent-facing tools: `task_create / task_list / task_delete` (the old `reminder_*` / `todo_*` set is gone). New backend dep: `croniter`.
- [20](./20-onboarding-diagnostics-docs.md) — onboarding, diagnostics, and docs — **diagnostics slice only** (2026-05-30; cross-repo [Holzi#50](https://github.com/haexhub/Holzi/pull/50) + [holzi-frontend#54](https://github.com/haexhub/holzi-frontend/pull/54) merged). `GET /api/diagnostics` ships six redacted subsystem checks (database, LLM credential, messenger, scheduler with `_task.done()` liveness probe, workspace roots, sandbox runtime — message says "configured" because the manager survives a dead Podman socket); `display_name` is run through a single-line, length-capped summariser and the workspace root list collapses to the first three + count, so user-controlled fields can't dominate the response. `/settings/diagnostics` replaces the placeholder stub with a flat status list + a "Letzte Fehlläufe" panel pulling `/api/runs?status=error` (rows expand to reveal the persisted trace, fallback when `error_trace=null`). `settingsNav.ts` drops the `upcoming` hint on the diagnostics entry; `statusLabel` / `statusBadgeClass` back onto `satisfies Record<DiagnosticsStatus, string>` maps so future schema additions are TS-enforced. **Deferred to follow-up slices**: persistent sandbox-crash log table (closed by 20-A below), first-run / onboarding empty state in `EmptyChatState.vue`, README quickstart + troubleshooting + provider-setup docs, optional bootstrap/check CLI. `/api/health` stays Plan 19's concern.
- [20-A](./20a-sandbox-crash-log.md) — persistent sandbox-crash log (2026-05-30; cross-repo [Holzi#51](https://github.com/haexhub/Holzi/pull/51) + [holzi-frontend#56](https://github.com/haexhub/holzi-frontend/pull/56) merged). Closes the Plan 11b-b "Known limit": every workspace dead-transition the health watcher fires is persisted into a new `sandbox_crashes` table by a lifespan-registered handler that subscribes **before** `start_health_watcher`. New `GET /api/sandbox/crashes` (auth-gated, `limit` clamped 1..100, newest-first via `(crashed_at DESC, id DESC)` so sub-second collisions still order correctly). `/settings/diagnostics` grows a third section "Sandbox-Abstürze" between Subsysteme and "Letzte Fehlläufe" — renders even when the live sandbox subsystem-check is green. `state` typed as `Literal["crashed", "oom", "removed"]` on the response so the frontend's `CRASH_STATE_LABEL satisfies Record<SandboxCrashState, string>` stays exhaustive; canonical writer is `_DEAD_STATES` in `sandbox/manager.py`, and adding a state is a documented five-step cross-system change. CodeRabbit was credits-exhausted → self-reviewed via two parallel general-purpose agents; four backend findings addressed in `1642021` before merge (db-None guard, `crashed_at` description, cross-system docstring, handler-isolation test). Live verification of the **endpoint shape** + dev-stack restart cycle confirmed after Plan 20-B (`/api/sandbox/crashes` returns `[]` clean on a fresh Docker-host stack); the full Podman crash → DB row → page row → restart-survives loop still needs a Podman host and remains deferred to the haex.cloud box.
- [20-B](./20b-devstack-docker-agnostic.md) — dev-stack docker-agnostisch (2026-05-30; cross-repo [Holzi#52](https://github.com/haexhub/Holzi/pull/52) + [holzi-frontend#58](https://github.com/haexhub/holzi-frontend/pull/58) merged). Makefile auto-detects docker first then podman (override via `CONTAINER_BIN=` / `COMPOSE_BIN=`); sandbox-only env + Podman control-socket mount moved to a new `docker-compose.local.podman.yml` overlay layered in only when `CONTAINER_BIN=podman`. Nested `${HERMES_PODMAN_SOCKET:-${XDG_RUNTIME_DIR}/podman/podman.sock}` flattened to a single `HERMES_CONTAINER_SOCKET` so compose v2 + podman compose 4.x parse it identically. `${HERMES_CONTAINER_SOCKET:-/var/run/docker.sock}` keeps hand-run compose working; `/run/user/$(id -u)` fallback in the Makefile covers empty `XDG_RUNTIME_DIR` (sudo / cron / minimal CI shells). Self-reviewed via two parallel general-purpose agents (CodeRabbit credit-exhausted); three backend findings + two frontend findings addressed in `256dba4` + `0155277` before merge. Live verification: `make up-local-full` succeeds on the user's Docker-only host; all five containers up, diagnostics returns `sandbox: warning ("HERMES_SANDBOX_SOCKET not set")`, frontend serves 200 OK at `app.localhost`, `make down-local` cleans up. Production compose (`docker-compose.yml`) unchanged — production sandboxing stays hard rootless Podman per Plan 11b-a.
- [20-C](./20c-onboarding-empty-state.md) — diagnostics-aware EmptyChatState (2026-05-30; frontend-only [holzi-frontend#60](https://github.com/haexhub/holzi-frontend/pull/60) merged). `EmptyChatState.vue` fetched `/api/diagnostics` when credentials are configured; renders a subtle banner under "Sag Hermes Hallo." with singular/plural copy + link to `/settings/diagnostics` when `overall !== 'ok'`. Credentials-missing state unchanged (priority CTA stays alone). Diagnostics fetch rejection falls through silently — diagnostics-down is an auth/connectivity problem, not an onboarding signal. Watcher is one-shot per become-true transition; the EmptyState unmounts on first message so a stale cache is a non-issue. Self-reviewed via a parallel general-purpose agent (CR still credits-exhausted); all NITs, "ship it"; one inline comment addressed in `9d591f6`. Live verified on the user's Docker host: banner showed "3 Subsysteme melden Warnungen oder Fehler" (messenger + workspace + sandbox = warning by default on a sandbox-less Docker dev host), href `/settings/diagnostics`. Tests: 6 new component cases (`hasCredentials` × diagnostics-overall + fetch-rejects fallthrough), vitest suite 196/196 green, typecheck green.
- [21](./21-approval-granularity.md) — approval granularity: once / session / always / deny + reason (2026-05-30; cross-repo [Holzi#53](https://github.com/haexhub/Holzi/pull/53) + [holzi-frontend#63](https://github.com/haexhub/holzi-frontend/pull/63) merged). New `tool_approvals` table (always-scope grants) + in-memory `session_approvals` map; the four-decision `ApprovalDecisionLiteral` (`allow_once`/`allow_session`/`allow_always`/`deny`) is centralised in `events.py`. `run_agent`'s gate calls `on_approval` only for `requires_approval` tools; the `/api/chat` handler emits `approval_required`, blocks on a per-id future resolved by `POST /api/approvals/{id}` (four-decision body + ≤500-char reason), and pre-empts the card when an always/session grant already covers the tool. `GET /api/approvals/standing` + `DELETE /api/approvals/standing/{tool}?scope=` manage grants. Frontend ApprovalCard gained the four decision buttons + a collapsible reason textarea; a deny reason flows into the tool result the LLM sees on its next round. This is the gate Plan 32-A's `mcp_install` rides.
- [22](./22-ui-polish-toaster-modals-dnd.md) — UI-Politur: Toaster, echte Modals, Composer-Drag&Drop (2026-05-30; frontend-only [holzi-frontend#62](https://github.com/haexhub/holzi-frontend/pull/62) merged). Replaces `window.confirm`/`window.prompt` with real components via new `useToast` / `useConfirm` / `usePromptDialog` composables + an `AppConfirmHost`; attachment drag&drop on `ChatComposer`. Existing `MemoryPage`/`TasksPage`/`WorkspacePanel` tests rewired from global stubs to `vi.mock('~/composables/useConfirm')`. Frontend 26 files / 213 tests green.
- [25](./25-multi-workspace-crud.md) — multi-workspace CRUD (2026-05-30; cross-repo [Holzi#55](https://github.com/haexhub/Holzi/pull/55) + [holzi-frontend#68](https://github.com/haexhub/holzi-frontend/pull/68) merged). Closes the last Plan-14 placeholder. New `workspaces` table + `routes/workspaces.py` (plural) shipping `GET/POST /api/workspaces`, `PATCH/DELETE /{id}`, `GET /{id}/disk`; the list response joins each row with a sandbox snapshot (`peek_workspace` + `status`, no side-effects), a 1-second-capped `du -sb` disk probe, and a lightweight git-branch/dirty probe so `/settings/workspaces` renders in one round-trip. Slug validation `^[a-z0-9][a-z0-9-]{0,62}[a-z0-9]$` (2..64 chars, kebab-case) is the only user-controlled value that ends up in `${sandbox_volume_root}/${id}`. `HERMES_WORKSPACE_ROOTS` env stays as an idempotent boot-time backfill; the DB table is the source of truth from here on. Frontend ships the two-pane Control Center page (list view + detail with rename inline, archive, sandbox-restart, sandbox/disk/git blocks); `WorkspacePanel.vue` chat-rail loads roots from `/api/workspaces` and projects to `{id}` for tree/file calls; empty-state points at `/settings/workspaces`. CI initially red on three ruff findings (unused `typing.Any`, `asyncio.TimeoutError` → builtin `TimeoutError` per UP041, unused `hermes.config` import in tests); all auto-fixable, fixed in `caab7f4`. Plan-12 singular `routes/workspace.py` still env-driven for now — rebasing onto the table is a deliberate follow-up. Hard-delete stays a non-goal. Full Podman-host UI driving deferred to the haex.cloud box.
- [25-A](./25a-workspace-source-of-truth-followups.md) — workspaces table is the request-time source of truth (2026-05-31; cross-repo [Holzi#59](https://github.com/haexhub/Holzi/pull/59) + [holzi-frontend#75](https://github.com/haexhub/holzi-frontend/pull/75) merged). Closes the two env-driven remnants Plan 25 deferred — fixes the user-visible regression where `/settings/diagnostics` kept warning `no workspace roots configured` even after a UI-create, AND every `/api/workspace/*` endpoint (Plan 12 read-only browser, Plan 13 write, Plan 24 git surface) returned 404 for workspaces created through the table. `_check_workspace` becomes async + reads `workspaces_repo.list_active(db)` with copy switching to `add one in /settings/workspaces`; `_configured_roots()` → `_active_root_slugs(db)`; `_require_known_root(request, root)` async + reads `app.state.db`; all 8 callers (4× tree/file, 4× write/rename/delete + git entry points) updated mechanically. `display_name` runs through the existing `_summarise(..., max_len=48)` helper so a long name can't dominate the diagnostics row. Test fixtures replaced: the `configure_roots(monkeypatch)` env-patcher in `test_api_workspace.py` + `test_api_workspace_git.py` became an async `configure_workspaces` that seeds via `workspaces_repo.create`; the env-string whitespace-parser edge test lost meaning and became a plain "two slugs visible" test (slug validation now happens at write time in `workspaces_repo.create`). `HERMES_WORKSPACE_ROOTS` env survives only as the lifespan boot-time backfill in `main.py` — nothing reads it at request time. Self-reviewed via two parallel `Explore` agents (first time using the read-only review-agent type per the lesson learned in Plan 27); zero blockers from either; one frontend commit-message accuracy fix amended (the regen delta is actually the `/api/workspace/roots` endpoint description, not the diagnostics message text — runtime field values aren't in OpenAPI). Backend 706 tests + ruff + mypy clean, frontend 260 tests + typecheck clean.
- [24](./24-workspace-git-extended.md) — extended workspace-git surface (2026-05-31; cross-repo [Holzi#56](https://github.com/haexhub/Holzi/pull/56) + [holzi-frontend#70](https://github.com/haexhub/holzi-frontend/pull/70) merged). Eleven new endpoints under `/api/workspace/git/*` (`diff` / `branches` / `log` / `checkout` / `stage` / `unstage` / `discard` / `commit` / `fetch` / `pull` / `push`), all routed through the workspace sandbox via the Plan-13 `_drain_exec(["git", ...])` helper — the agent host never touches the working tree. Diff returns `kind: text|binary|none` with summary from a separate `--numstat` pass and a 256 KiB patch cap (summary stays authoritative). Pull conflict enumeration uses `git diff --name-only --diff-filter=U` as the authoritative source (initial line-parser version was scrapped pre-merge after self-review showed it returned branch names instead of paths for add/add + modify/delete + rename/rename CONFLICT variants). New `HERMES_WORKSPACE_GIT_DESTRUCTIVE` env gates `/discard` and `checkout --force` with 403 by default; dirty checkout without `force` returns 409. Argv hardening: branch names + paths starting with `-` rejected at the input layer (`_normalise_relative` now refuses leading `-`) so forgotten `--` separators can't become flag-injection. Frontend adds `WorkspacePanel` `Dateien | Git` tab strip + new `WorkspaceGitTab.vue` (branch dropdown with `+ Neuen Branch erstellen…` prompt, Unstaged/Staged sections with per-row stage/unstage/discard, diff viewer using a CommonMark-correct dynamic backtick fence — longest run + 1 — so patches against `.md` files with their own ` ``` ` blocks render correctly, commit row, Fetch/Pull/Push with inline conflict list); dirty badge in the Files tab is a clickable button that jumps to the Git tab. Self-reviewed via two parallel general-purpose agents (CR rate-limited on both PRs); review surfaced two backend blockers (branch-flag + path-flag injection), one should-fix (pull conflict parser), three frontend correctness bugs (branch-select desync on cancel/409, fence-escape on triple-backtick patches, stale selection after stage/unstage) — all five addressed in `77026c0` + `b38bf34` before merge. CI green on both; backend 685 tests + ruff + mypy clean, frontend 238 tests + typecheck clean. Deferred to follow-ups: agent-suggested commit messages (LLM call), "Origin ahead N, behind M" indicator, log-list UI (backend endpoint shipped, UI not yet), live push/pull against a real `git daemon` (backend tests script stderr through `FakeSandboxBackend`).
- [27](./27-insights-and-logs.md) — Insights + Logs viewer (2026-05-31; cross-repo [Holzi#57](https://github.com/haexhub/Holzi/pull/57) + [holzi-frontend#72](https://github.com/haexhub/holzi-frontend/pull/72) merged). Two new Control Center pages on top of two read-only endpoints: `GET /api/insights?period=24h|7d|30d` aggregates `agent_runs` into totals + zero-filled daily UTC buckets + per-model + per-status (new `runs.aggregate_*` helpers, pure SQL via `func.sum/count` + `strftime`); `GET /api/logs?tail=&min_level=` tails `HERMES_LOG_FILE` (503 when unset, 1000-row cap, level filter, malformed lines pass through as `{_raw: "..."}`). Logging gains a `_redaction_processor` + `RotatingFileHandler` sibling to stdout. 7d/30d SQL cutoff is anchored to the same UTC midnight as the rendered series labels so a row on the oldest partial UTC day can't count in totals while being absent from the chart; 24h stays rolling by design. Secret-key redaction now covers three paths: `redact_secrets()` for JSON keys (write-time processor + read-time defensive pass), and `redact_secrets_in_text()` for the `_raw` fallback (catches inline `Bearer sk-xxx` from uvicorn / httpx / MCP-startup stdlib records that bypass the JSON-key matcher). Frontend: `/settings/insights` renders KPI tiles + Tailwind-only bar chart (no chart lib) + sortable per-model table (sort `<th>`s are real `<button>`s with `aria-sort`, keyboard-accessible) + status counts, auto-refresh 60s while visible; `/settings/logs` adds severity/tail/substring filters, copy-all, wrap toggle, auto-refresh 5s. `app/lib/pricing.ts` is the static USD per-1M-token table for the cost-estimate tile, with a `normalisedModelId()` helper that strips trailing `-YYYYMMDD` so versioned ids still match short-alias entries. Review trail: CR rate-limited initially; one backend `general-purpose` self-review agent overstepped and pushed two follow-up fix commits (kept — improvements were defensible), lesson recorded as "use `Explore` for review agents, not `general-purpose`". Later CR pass web-fact-checked three stale pricing rates (Opus 4.x $5/$25 not the 3.x $15/$75, GPT-4o $2.50/$10, Gemini 1.5 Pro ≤128k $1.25/$5); two nitpicks about German error fallbacks declined as `'Fehler beim Laden.'` matches the convention in 4 other composables. Backend 705 tests + ruff/mypy clean, frontend 260 tests + typecheck clean; live smoke against `HERMES_LOG_FILE`-enabled backend confirmed honest zero-filled buckets, real `hermes_starting` rows surfaced, and 400/401/503 paths all fire.

- [31](./31-tool-inventory-and-mcp-surface.md) — Tool-Inventar + MCP-Surface (2026-06-03; cross-repo [Holzi#65](https://github.com/haexhub/Holzi/pull/65) + [holzi-frontend#83](https://github.com/haexhub/holzi-frontend/pull/83) merged). Kills the last `/settings/skills` placeholder — the final shipped section in the Control Center. Backend adds two small read-only endpoints: `GET /api/tools` projects `app.state.tool_catalog` to `ToolsResponse { tools: ToolInfo[], total }`, sorted alphabetically; `ToolInfo` carries name/description/requires_approval/risk_reason/parameters_schema/source. The endpoint reads the same list MCP exposes (no second build path, no helper duplication). `source` is statically `"builtin"` today and intentionally free-form `str` (not `Literal`) so Plan 32 can drop `"mcp:<server-name>"` into the catalog and have the field pass through without breaking the OpenAPI contract. `GET /api/mcp/health` is state-only (`app.state.mcp_manager` presence + `len(tool_catalog)` + static `url="/mcp"`); a real HTTP self-probe was rejected because it would re-enter the ASGI stack and the `StreamableHTTPSessionManager` refuses to short-circuit without a full MCP handshake. Both endpoints stay behind the existing global bearer middleware; no new public paths. Frontend `/settings/skills` replaces the placeholder with two sections: an MCP-Surface card (status pill, `/mcp` URL with copy-to-clipboard, exponierte Tool-Count, „vor X s" relative timestamp from a 1-Hz `now`-ref updater, Refresh-Button, Setup-Hinweis on Bearer auth, disabled „MCP-Server konfigurieren"-Button als Plan-32-Sprungpunkt) and a flat alphabetical Tool-Katalog (monospace name + `source`-Pille + Approval-Badge wenn `requires_approval` + risk_reason text gated on the same flag, JSON-pretty-printed `parameters_schema` viewer behind a toggle with „keine Parameter"-Fallback wenn `properties` leer, disabled per-Tool „Konfigurieren"-Button mit Plan-32/33-Tooltip). Two ~25-line composables (`useTools`, `useMcpHealth` with a `lastCheckedAt` ref) wrap the endpoints; sections own independent loading/error refs so one endpoint failing never hides the other. `settingsNav.ts` drops the `upcoming` hint for `/settings/skills`; the matching `SettingsPlaceholder.test.ts` shipped-routes assertion is updated. Review trail: CodeRabbit rate-limited on both PRs at submit time, two parallel `Explore` review-agents found zero blockers; a NIT-fix pulled in (`parameters_schema: dict[str, Any]` for consistency with every other route in `src/hermes/routes/`). Backend 773 tests + ruff/mypy clean, frontend 280 tests + typecheck clean.
- [32](./32-mcp-server-crud.md) — MCP-Server CRUD + Lifecycle (2026-06-03; cross-repo [Holzi#66](https://github.com/haexhub/Holzi/pull/66) + [holzi-frontend#85](https://github.com/haexhub/holzi-frontend/pull/85) merged). Activates the disabled „MCP-Server konfigurieren"-Sprungpunkte from Plan 31 and unlocks Plan 32-A's self-provisioning. New `mcp_servers` table (kebab slug 2..32, transport `http`/`stdio`, `url` | `command_argv` JSON, opaque `env_json`, AES-GCM `credentials_*` tripel re-using `crypto.Encryptor`, `enabled`/`last_error`/timestamps + `idx_mcp_servers_enabled`). `repository/mcp_servers.py` is the secret boundary: the public `McpServer` dataclass exposes only `env_keys` (variable names, no values) and never returns plaintext credentials — an internal `read_secrets(engine, id, encryptor) -> McpServerSecrets` is the single decrypt path the manager uses. New `mcp_manager.py` owns one `asyncio.Task` per server that holds `streamablehttp_client`/`stdio_client` + `ClientSession` open through an `AsyncExitStack` until `stop_event` fires; handles surface `starting`/`ready`/`crashed`/`disabled` (never auto-restart — same posture as Plan 11b-b sandbox crashes). Tool handlers look up the live session via `manager.get_handle(server_id)` on every call so restarts don't strand the catalog, and wrapped tools are namespaced `{server_name}__{tool_name}` with `source=f"mcp:{server_name}"`. `Tool` dataclass gained `source: str = "builtin"` (Plan 31 reserved the slot; Plan 32 first writes non-default values); `tool_catalog.build_tool_catalog(..., mcp_manager=...)` merges aggregate output after the built-ins. `routes/mcp_servers.py` ships list/create/PUT/delete/restart/per-server-health with sentinel PUT semantics (omitted = unchanged, explicit `null` = clear, value = set; `_changed_keys(request)` inspects raw body to disambiguate); each mutation calls `_refresh_catalog(request)` so `app.state.tool_catalog` is re-assembled and the next request sees the new tool set. `routes/mcp_health.py` extended with per-server summary (legacy keys preserved → Plan 31 card still renders unchanged); `routes/tools.py` now passes `t.source` through instead of hard-coding `"builtin"`. Lifespan constructs the manager after the `Encryptor` is ready, calls `start_all_enabled()` before assembling the catalog, and `stop_all()` on shutdown. Frontend `/settings/skills` mounts a new `McpServersSection.vue` at the top: list of server cards (status pill + transport pill + `mcp:<slug>` source + url-or-argv + `env_keys` list + collapsible `last_error`), Restart / Toggle / Edit / Delete buttons each with their own single-flight latch (`restartingId` / `togglingId` / `deletingId`), 10s `setInterval` auto-refresh that pauses on `document.visibilityState !== 'visible'`. Inline create/edit form gates http-vs-stdio fields via a transport toggle, env-vars as key/value pairs (values `type="password"`), Bearer-Token field with reveal toggle and explicit „Gespeichertes Token entfernen" button that sends `credentials: null`; on edit the credentials and env values stay empty by design (the user retypes to rotate) and a `credentialsTouched` sentinel decides whether to include the field at all. Plan 31's `[data-testid="mcp-configure"]` button activated (scrolls to `#mcp-section`); per-tool `[data-testid="tool-configure-<name>"]` for `mcp:<name>`-sourced tools active (scrolls to the matching card), built-in stays disabled (Plan 33). `useMcpServers` composable + new `McpServer*` schemas via `pnpm run gen:api`. Review trail: CodeRabbit rate-limited again, two parallel `Explore` review-agents found one blocker (CI mypy: `Event | None` everywhere — fixed by `field(default_factory=asyncio.Event)`), one NIT (local `build_tool_catalog` import hoisted to module top after verifying no cycle), one test-gap (paired the credentials-null PUT test with an omitted-credentials test that asserts the ciphertext survives untouched), and one frontend gap (`deletingId` single-flight to match restart/toggle). Backend 836 tests + ruff + mypy clean, frontend 293 tests + typecheck clean. Live verification against `npx -y @modelcontextprotocol/server-filesystem /tmp` deferred to post-merge smoke pass.
- [26](./26-conversation-deep-links.md) — Conversation Deep-Links (2026-06-04; frontend-only [holzi-frontend#88](https://github.com/haexhub/holzi-frontend/pull/88) merged). `/chat/[id]` Nuxt-Route + `/` → `/chat/<id>`-Redirect bei Last-Active-Restore + Browser-Title pro Conversation. Chat-Shell aus `pages/index.vue` in neues `app/components/ChatHub.vue` extrahiert (~990 Z., kept mechanical), beide Routen rendern denselben Hub. `holzi.lastConversationId` persistiert über einen neuen Pinia-Store `app/stores/lastConversation.ts` analog zu `auth.ts` (VueUse `useLocalStorage` + Custom-Serializer der NaN/Legacy/Garbage zu `null` normalisiert — sonst hätte VueUse's Default-Number-Serializer den `=== null`-Guard in `index.vue` mit NaN umgangen und `/api/conversations/NaN` aufgerufen). `loadingConversation`-Guard auf `ChatHub` unterdrückt den `EmptyChatState`-Flicker auf Cold-Deep-Link-Load (`true` pre-seeded wenn ein nicht-null `conversationId` reinkommt, in `loadConversation` + `newChat` + der `route.params.id`-watch-Branch korrekt resetted). `useHead({ title })` pro Deep-Link, damit Multi-Tab-Workflows nicht alle Tabs auf "Neuer Chat" zeigen. Review-Trail: Plan-Subagent + read-only `Explore`-Review-Subagent dispatched in derselben Welle wie 28 + 20-followup-docs; Reviewer fand 2 SHOULD-FIX (Flicker + Title) + 1 NIT (Doppel-Fetch — akzeptabel) + 1 QUESTION; User-Pushback gegen das naked-`localStorage`-Pattern führte zur Pinia-Migration; alles in einem Review-Fix-Commit `0ad755e` adressiert. CodeRabbit auf beiden Stages rate-limited (Skills-Sprint hat das Hour-Budget aufgebraucht). 38 files / 328 tests + typecheck green. Out-of-scope-Followups: per-Message-Anchor-Links (`#msg-<mid>`) + `ChatHub`-Split in eigene Composables (tool-card-state, streaming-state) — beide explizite Non-Goals von Plan 26.
- [34](./34-remove-messengers.md) — Messenger-Familie komplett entfernt (2026-06-04; cross-repo [Holzi#71](https://github.com/haexhub/Holzi/pull/71) + [holzi-frontend#89](https://github.com/haexhub/holzi-frontend/pull/89) + [ansible#48](https://github.com/haexhub/ansible/pull/48) merged). Architektonische Korrektur nach Plan 28: Signal-Note-to-Self-via-Linked-Secondary war durch `signal-cli`'s syncMessage-Filter strukturell blockiert, der Quadlet-MODE-Flip allein hätte das nicht gelöst, und Telegram-Bots widersprechen dem Privacy-Anspruch des Projekts. Statt eine eigene Primary-Signal-Number zu beschaffen wurde die gesamte Messenger-Surface (Signal + Telegram) aus Backend, Frontend und Ansible hart-removed. `CHANNEL_REGISTRY` schrumpft auf `{web, task}`; `messenger_accounts`-Tabelle + alle Signal-/Telegram-Rows in `conversations` werden in der Lifespan-Migration gedroppt. Frontend verliert `/settings/messenger` + `useMessenger` + alle `MessengerAccount*`-Types. Ansible löscht `signal-cli-rest-api`-Quadlet + Cleanup-Task fährt den laufenden Container auf der Box runter (idempotent, no-op auf Boxen ohne Signal-Deploy). Plan 28 (Signal-WS, 2026-06-04 gemerged) ist damit retro-aktiv obsolet — der Code lebt nur in der Git-Historie. Review-Trail: CodeRabbit + 2 parallele `Explore`-Reviewer fanden 1 BLOCKER pro Repo (40 leftover channel-literals BE, orphaned `TelegramAccountCreate` FE) + Nits (stale docstrings, dead secrets-example key); alle adressiert. BE 858 tests, FE 322 tests, Ansible idempotent. Box-Lauf via `ansible-playbook ... --tags 'holzi,holzi-frontend' -K` durch den User.
- [28](./28-signal-websocket-receive.md) — Signal Note-to-Self via WebSocket — **OBSOLET durch Plan 34 (2026-06-04)**. Wurde am 2026-06-04 als [Holzi#69](https://github.com/haexhub/Holzi/pull/69) gemerged, aber nie aktiviert (Ansible-Quadlet-Flip deferred + Linked-Secondary-Limitation hätte json-rpc-Mode auch nicht gelöst). Code lebt nur in der Git-Historie. `SignalClient.receive_stream` öffnet WebSocket gegen `/v1/receive` von signal-cli-rest-api im json-rpc-Modus (vorher: HTTP-Polling, signal-cli ignoriert das im json-rpc-Modus stillschweigend → die ganze Signal-Receive-Schiene hat überhaupt nicht funktioniert). `SignalWorker._run()` läuft jetzt die WS in einer reconnect-Schleife mit exponential-capped Backoff (1→2→5→10→30s, bei Persistenz auf 30s gepinnt), `asyncio.CancelledError` re-raise propagiert sauber bis zum Lifespan-Shutdown. Auth-Gate `_extract_self_text` filtert Note-to-Self aus sync- + data-Message-Envelopes (Path A = primary device, Path B = same number from another device) und rejected fremde `source` / `destination`-Felder; 5 Boundary-Tests + 2 JSON-Fixtures (`tests/fixtures/signal_envelopes/note_to_self_{sync,data}.json`) basiert auf der dokumentierten signal-cli-JSON-RPC-Contract (Issues #797 + #840) — Live-Envelope-Diff (Task 4) für die haex.cloud-Box deferred. `websockets≥12` als Dep hinzugefügt. Memory `reference_signal_linked_secondary_limits` (Signal-Linked-Devices empfangen kein Note-to-Self im REST-Modus) IST WEITER GÜLTIG für REST-Modus, aber dieser Plan rüstet json-rpc-Modus + WS nach — Aktivierung braucht den Ansible-Flip (Task 8). Review-Trail: prior session lieferte 3 Commits (deps, `receive_stream`, worker-loop) mit veralteter Base, neuer Subagent rebased clean auf current main + Task-4-Fixtures + `_extract_self_text` + 5 Tests in `3d071b7` + `2106ac7`; read-only `Explore`-Reviewer fand 1 SHOULD-FIX (non-dict + malformed JSON wurden silent dropped → `logger.debug` in `2311eb2`) + 1 NIT zu Path-B-Fallback-Docstring-Klarstellung; CodeRabbit rate-limited. **Noch offen (Tasks 8–10):** Ansible-Quadlet-Flip auf `MODE=json-rpc`, Live-Verifikation auf haex.cloud-Box, Memory + Roadmap-README-Rewrite nach Box-Verifikation — bewusst deferred bis ein Maintenance-Fenster für die Box offen ist.
- [20-Docs-Slice](./20-onboarding-diagnostics-docs.md) — Plan-20-Follow-up Docs-Slice (2026-06-04; backend-only [Holzi#70](https://github.com/haexhub/Holzi/pull/70) merged). Schließt die letzte Plan-20-Lücke: README-Quickstart komplett überarbeitet gegen die tatsächlichen Makefile-Targets (`install` / `token` / `up-local` / `up-local-full` / `down-local`), die echte `*.localhost`-Traefik-Routing (`app.localhost` → UI, `app.localhost/api` → backend, gleicher-Origin → kein CORS), und den Bearer-Token-paste-into-UI-Flow (Token persistiert in `localStorage` via `useAuthStore`). Neu: `docs/providers.md` mit per-Provider-Setup für die fünf literal `ProviderLiteral`-Werte aus `routes/llm.py` (`anthropic` api_key + `oauth_claude`, `openai`, `openrouter`, `google`, `custom`) inklusive der `HERMES_LLM_URL` / `HERMES_LLM_API_KEY` / `HERMES_MODEL`-Env-Fallback-Story; klargestellt dass `custom` keinen `/v1/models`-Lister hat. Neu: `docs/troubleshooting.md` mit 11 Diagnosen (Auth-Token-Missing-Boot-Crash, 401-Loop auf UI, FE↔BE-Reachability, LLM-No-Credential vs Upstream-401, Sandbox `warning` vs `error` mit Rootless-Podman-Host-Requirements, leerer Workspace-Browser, MCP-Restart, Signal-Note-to-Self-Silent mit Linked-Secondary-Limitation, `/api/logs`-503, FE-`node_modules`-Drift nach `frontend-reinstall`, `make clean` zerstört `hermes.db`). Stale-Links im alten README (kaputter `haex-claude-proxy`-Link, Cline/Roo-Referenz, "Phases 0–4"-Status) ersetzt. Review-Trail: read-only `Explore`-Reviewer fact-checkte gegen den tatsächlichen Code (alle 8 zitierten Makefile-Targets existieren, alle env-vars matchen `config.py`, alle 5 Provider-Literale matchen `routes/llm.py`); flagged 1 vermeintlichen BLOCKER zur Sandbox-Crash-Log-Doku, der sich als Reviewer-Misread herausstellte (Doku sagt `/settings/diagnostics`-Page nicht `/api/diagnostics`-Endpoint — beide existieren, der `useDiagnostics`-Composable holt beide) — Doku trotzdem präzisiert in `83b8006` um beide Surfaces explizit zu nennen. CodeRabbit rate-limited.
- [33](./33-skills-as-db-artifacts.md) — Skills als DB-Artefakte (2026-06-03/04; cross-repo [Holzi#68](https://github.com/haexhub/Holzi/pull/68) + [holzi-frontend#87](https://github.com/haexhub/holzi-frontend/pull/87) merged). Closes the Skills & Tools trio's third leg. New `skills` table (id, kebab-slug 1..64, name, description, optional `when_to_use`, ≤16 KiB `body_markdown`) + `persona_skills` link table (`ordering`, `enabled`, both FKs CASCADE) with a `(persona_id, ordering)` index for the resolver's hot query. `repository/skills.py` ships CRUD + `list_for_persona` + an atomic `set_persona_skills` (single tx: DELETE-all-for-persona + INSERTs, so an unknown `skill_id` rolls back the entire replace and the persona keeps its prior list). `hermes/personas.py`'s `get_effective_system_prompt` now composes `persona + active_skills + capability_index + channel`: skills with `enabled=False` or whitespace-only bodies are silently skipped, and a persona with zero skills produces the exact same prompt as before Plan 33 (all 10 pre-existing resolver tests stay green). `routes/skills.py` (`GET/POST/PUT/DELETE /api/skills`) maps duplicate slug → 409 via `IntegrityError`, slug regex `^[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?$` (1..64 chars, single-char deliberately allowed) + body 1..16 KiB at the Pydantic layer; `routes/preferences.py` grows `GET/PUT /api/personas/{id}/skills` returning the embedded `SkillResponse` per row so the UI renders without a second round-trip, and the PUT's `IntegrityError → 422` catch covers both unknown `skill_id` (FK violation) and duplicate `(persona_id, skill_id)` (PK conflict). Frontend `/settings/skills` opens with a new `SkillsSection` (two-pane mirroring `/settings/memory`: searchable list left, read mode with `RenderedMarkdown` + edit form right, slug-immutable on update, live length counter with 8 KiB soft warning + 16 KiB hard cap, prominent always-visible amber security notice in the section header — moved out of the form per review so viewers see "no secrets" even in read mode); the MCP-Server + Tool-Katalog sections from 31/32 stay below. `/settings/preferences` persona cards get an "Aktive Skills" sub-block: checkbox toggle, up/down reorder buttons (DnD upgrade is a future plan), remove button, and a "Skill hinzufügen" dropdown filtering out already-active skills. Pessimistic update: `persistPersonaSkills` only swaps local state to `resp.skills` after a successful PUT, so a 422 leaves the UI on the prior list — pinned by a regression test. `useSkills` composable wraps the new endpoints; `pnpm run gen:api` picked up `SkillResponse`, `PersonaSkillItem`, `PersonaSkillSetRequest`. Review trail: parallel `Explore` review-agent found one "blocker" (state-revert on failure) that turned out to be a misread of the already-pessimistic flow — added a regression test pinning the contract instead; nits addressed (slug-comment cleanup, security-notice always-visible). CodeRabbit rate-limited on the frontend PR and pending on the backend at merge time; CI green on both (backend 910 tests + ruff + mypy clean, frontend 35 files / 315 tests + typecheck clean). Live: created skill `strict-german` via API, activated on default persona, called `get_effective_system_prompt('web', engine)` — body appeared between persona and capability index for all four channels; disabling omits the body; deleting cascades the persona-skills link.
- [32-A](./32a-agent-self-inventory.md) — Agent-Self-Inventory + Self-Provisioning (2026-06-03; cross-repo [Holzi#67](https://github.com/haexhub/Holzi/pull/67) + [holzi-frontend#86](https://github.com/haexhub/holzi-frontend/pull/86) merged). Closes the Skills & Tools trio: four agent meta-tools in `src/hermes/tools/meta.py` (`build_meta_tools(db, mcp_manager, encryptor, tool_catalog_provider)`) so "configure Holzi via Holzi" works conversationally as well as via `/settings/skills`. `list_tools` (no approval) mirrors `app.state.tool_catalog` via a provider read at call time (optional `source_filter`, lists itself — transparency over a recursion filter); `mcp_status` (no approval) merges `repo.list_all()` with `manager.list_handles()` so disabled/unbooted servers still report; `mcp_install` (approval-gated, `risk_reason`) validates → encrypts creds → `repo.create` → `start_server` under `asyncio.wait_for(15s)` with full cleanup (`stop_server` + `delete`, logged) on timeout/crash so no zombie row, returning a secret-free summary + `tools_added`; `mcp_restart` is deliberately **no-approval** (mirrors the UI restart button — supersedes the plan body's `requires_approval=True`, so the planned frontend restart-card variant is dead and wasn't built). Redaction contract: `redact_mcp_install_params` is the single source, wired as a new `Tool.redact_arguments` callable; `agent.run_agent` computes `display_args` once and routes it to the approval callback, the `tool_call` SSE event, and **both** persisted `messages.meta_json` sites — the tool-turn `arguments` and a redacted copy of the assistant turn's `tool_calls` via `_redact_persisted_tool_calls` — while raw args still reach the handler + the upstream request. No new gate — rides Plan 21. `build_tool_catalog` gained optional `encryptor` + `tool_catalog_provider`; the lifespan wires the manager's `on_catalog_change` and a single `_live_catalog` provider so agent-driven installs refresh `app.state.tool_catalog` (the route CRUD path already did). Frontend adds `McpInstallApprovalDetails.vue` (transport pill + URL/command + env-**key** list + "credentials set" marker — only ever sees backend-redacted values) and branches `ApprovalCard.vue` on `name === 'mcp_install'`, keeping the generic JSON block for every other tool. Review trail: CodeRabbit (this time un-rate-limited) flagged three Major points, all fixed — (1) `_redact_persisted_tool_calls` could persist raw secrets when `function.arguments` arrived as a dict (non-streaming providers) or unparseable string → now redacts dict args directly and writes a `{"_redacted": true}` placeholder, never raw; (2) the inbound `/mcp` StreamableHTTP server snapshotted its tools + `lookup` at mount → `build_mcp_server` now takes a `tools_provider` read live in `list_tools`/`call_tool`, bound to `_live_catalog`, so a runtime-installed server appears on `/mcp` without a restart (was a latent pre-existing Plan-32 bug); (3) silent install-cleanup failures now log. A follow-up independent `Explore` review raised two more, both verified false positives (a boot-time `AttributeError` that can't happen — `tool_catalog` is `[]` from lifespan start and the provider isn't called during build; and a per-call lookup rebuild that's the intentional cost of live reads). Backend 865 tests + ruff + mypy --strict clean, frontend 301 tests + typecheck clean. No `gen:api` (no new endpoints).
- [29-A](./29a-personas-and-channels.md) — Personas-Core + Channel-Prompts (2026-06-01; cross-repo [Holzi#61](https://github.com/haexhub/Holzi/pull/61) + [Holzi#62](https://github.com/haexhub/Holzi/pull/62) (response_model follow-up) + [holzi-frontend#79](https://github.com/haexhub/holzi-frontend/pull/79) merged). Two new tables (`personas` + `channel_prompts`), single-default-persona invariant via SQLite triggers, `ON DELETE SET NULL` on `channel_prompts.default_persona_id`. Lifespan calls idempotent `ensure_backfill` that seeds the default Hermes persona + one row per `CHANNEL_REGISTRY` key (`web` / `task` / `signal` / `telegram`). `get_effective_system_prompt(channel, db)` composes `persona.prompt + "\n\n" + channel.prompt`; the four call-sites (`routes/api.py`, `scheduler.py`, `main.py` × 2) lost their inlined `*_SYSTEM_PROMPT` constants. New `routes/preferences.py` with typed Pydantic response models so `gen:api` emits proper `Persona` / `ChannelPrompt` shapes (added in #62 after #61's squash dropped them in a working-tree-dirty race — lesson recorded as `feedback-gh-pr-merge-dirty-tree`). Frontend `/settings/preferences` is a two-section page (Personas: inline CRUD + Default-Badge + "Als Default setzen" + delete-disabled-for-default; Channels: one card per registry entry with persona dropdown + prompt textarea + "Prompt zurücksetzen" when divergent). The `useChannels`/`usePersonas` composables wrap the new endpoints. Code-review findings addressed in the fix-up commit: channel-watch now preserves in-flight drafts across `load()` (previously a persona mutation silently wiped unsaved channel-prompt edits) and `setDefaultPersona`/`deletePersona` gained a `personaMutating` latch against double-click concurrent PUTs. Backend 757 tests + ruff/mypy clean, frontend 269 tests + typecheck clean; live smoke confirmed backfill + create + promote + 422 on default-delete + channel edit + reset + 404 on unknown channel.

**Plan 34 done 2026-06-04** — Messenger-Familie (Signal + Telegram) komplett entfernt; Holzi ist jetzt ein Web-UI + Scheduled-Tasks-Agent. **Wave 1 done 2026-06-04** — Plan 26 (deep-links), Plan 28 (Signal-WS — durch Plan 34 obsolet), Plan 20-Follow-up-Docs merged.

**Wave 0 done 2026-06-04** — [Plan 30](./30-i18n-foundation.md) (i18n Foundation): bilingual UI + `ErrorCode`-Enum + `translateError` + 113 ErrorCodes. Cross-repo [Holzi#73-area] + [holzi-frontend#90-area] merged.

**Wave A1 done 2026-06-05** — [Plan 36](./36-personas-fragments.md) (Persona-Fragments + `persona_history`): `soul`/`identity`/`agents`-Spalten + Audit-Trail. Cross-repo BE PR#72 squash `057a1cc` + FE PR#90 squash `7ec8c46` + Follow-ups PR#73/PR#91/PR#92 merged. 884 pytest + 461 vitest.

**Wave A2 done 2026-06-05** — [Plan 37](./37-skill-catalog-bootstrap.md) (Skill-Catalog lazy-load + Bootstrap-Skill): `persona_skills` gedroppt, globaler Catalog-Index, `skill_load`/`skill_search`/`persona_update`/`mark_bootstrap_complete` Tools, `bootstrap-first-chat` Skill, `users`-Tabelle. Cross-repo BE PR#74 + FE PR#93 — 904 pytest + 459 vitest.

**Next up: Wave A3** — [Plan 38](./38-starter-skills.md) (Curated Starter-Skill-Library, ~6–10 Seed-Skills als Catalog-Entries). Dann Wave B: Plan 29-D-Resurrect (Modell pro Persona + Failure-UX).

### Preferences Family (Plans 29–30)

Triggered by the `/settings/preferences` placeholder needing real content
(2026-06-01). Per-session scope, in dependency order:

- [29-A — Personas-Core + Channel-Prompts](./29a-personas-and-channels.md) —
  `personas` + `channel_prompts` tables, CRUD, `/settings/preferences` two-
  section UI, composition `persona.prompt + "\n\n" + channel.prompt`.
  Implements the foundational data model. **Start here.**
- [29-B — Persona pro agent_task](./29b-persona-per-task.md) — Dropdown
  im Task-Form + `agent_tasks.persona_id`.
- [29-C — Persona pro Conversation](./29c-persona-per-conversation.md) —
  Composer-Chip im Web-Chat + `conversations.persona_id`.
- [29-D — LLM und Modell pro Persona](./29d-persona-llm-and-model.md) —
  `personas.llm_credential_id` + `personas.model`, Resolver liefert
  `PersonaContext` mit credential/model, zwei Dropdowns pro Persona-Card.
- [29-E — Tools/MCPs pro Persona](./29e-persona-tools-and-mcps.md) —
  `personas.tool_allowlist` (JSON), `filter_catalog`-Helper,
  `GET /api/tools` für den Multi-Select-Picker.
- [30 — i18n-Foundation (DE/EN)](./30-i18n-foundation.md) — `@nuxtjs/i18n`,
  String-Extraktion, Sprach-Picker in Preferences, Backend-`ErrorCode`-
  Enum + FE-`translateError`-Helper. **Komplett auf `main` 2026-06-04**
  (Tasks 1+2+3+4+5; final commits `893991d` BE + `5f9f042`/`dc76475` FE).
  Wave 0 von [Plan 35](./35-strategic-roadmap-2026h2.md).

### Skills & Tools Family (Plans 31–33)

Triggered by the `/settings/skills` placeholder needing real content
(2026-06-03). Per-session scope, in dependency order:

- [31 — Tool-Inventar + MCP-Surface](./31-tool-inventory-and-mcp-surface.md) —
  read-only `GET /api/tools` + `GET /api/mcp/health`, `/settings/skills`
  rendert flache alphabetische Tool-Liste mit `source`-Pille +
  MCP-Health-Card + deaktivierte Konfigurieren-Sprungpunkte. Liefert
  gleichzeitig die Datenquelle, die [29-E](./29e-persona-tools-and-mcps.md)
  als Multi-Select-Picker konsumiert. **Merged 2026-06-03.**
- [32 — MCP-Server CRUD](./32-mcp-server-crud.md) — externe MCP-Server
  registrieren, Lifecycle (start/stop/restart), Credentials, exponierte
  Tools in den Catalog mergen (mit `source="mcp:<name>"`). Aktiviert die
  Konfigurieren-Sprungpunkte aus Plan 31. **Merged 2026-06-03.**
- [32-A — Agent-Self-Inventory + Self-Provisioning](./32a-agent-self-inventory.md) —
  Meta-Tools `list_tools` / `mcp_status` / `mcp_install` / `mcp_restart`,
  sodass der Agent auf Nutzer-Fragen wie „hast du Service X?" antworten
  und (mit Approval) selbst MCP-Server einrichten kann. Hängt an Plan 32
  (Backend-CRUD muss zuerst existieren) und an Plan 21 (Approval-
  Granularität für `mcp_install`). **Merged 2026-06-03.**
- [33 — Skills als DB-Artefakte](./33-skills-as-db-artifacts.md) —
  Anthropic-Skill-Modell (Markdown + Frontmatter), Storage,
  Persona-Aktivierung, Resolver komponiert `persona + skills + channel`.
  Hängt an 29-A. **Merged 2026-06-03/04.**

### Parity Push (Plans 21–27)

Based on a three-way audit of holzi-frontend vs. the Holzi backend vs.
`nesquena/hermes-webui` (2026-05-30), these seven session-sized plans close the
biggest "you-see-it-immediately" parity gaps. They are roughly ordered by
"impact-per-session" — pick whichever fits the next session.

**As of 2026-06-04: 21, 22, 24, 25, 26, 27 are merged (see Completed above); only 23 remains open in this push.**

- [21 — Approval-Granularität](./21-approval-granularity.md) — once/session/always/deny + reason. Small BE+FE, big UX.
- [22 — UI-Politur](./22-ui-polish-toaster-modals-dnd.md) — Toaster, echte Modals (kill `window.confirm`/`prompt`), Drag&Drop für Attachments. Frontend-only.
- [23 — Composer-Chips](./23-composer-chips.md) — Model + Reasoning-Stufe (5-level) + Workspace pro Conversation. BE+FE.
- [24 — Workspace Git extended](./24-workspace-git-extended.md) — Diff/Stage/Branch/Commit-Compose/Push/Pull on top of Plan 13's auto-commit. BE+FE.
- [25 — Multi-Workspace CRUD](./25-multi-workspace-crud.md) — Workspaces als DB-Row, `/settings/workspaces` als echte Seite, per-workspace Sandbox + Disk + Git Status. BE+FE.
- [26 — Conversation Deep-Links](./26-conversation-deep-links.md) — `/chat/[id]`-Route + reload-stabile URLs. Frontend-only.

Strategic-judgement plans (not part of this push, separate decisions):

- **Skills / System-Prompts in DB** — system-prompt half is closed by [29-A](./29a-personas-and-channels.md); the skills half is [Plan 33](./33-skills-as-db-artifacts.md) in the [Skills & Tools family](#skills--tools-family-plans-3133) below.
- ~~**i18n foundation**~~ — done 2026-06-04, siehe [Plan 30](./30-i18n-foundation.md). Wurde 2026 zur Wave-0-Foundation des [strategischen Roadmap-Refactors (Plan 35)](./35-strategic-roadmap-2026h2.md), weil OSS-Reach ohne EN-UI nicht skaliert.
- **Kanban / Voice / Embedded Terminal / Plugins / Passkeys / Self-Update** — hermes-webui ships these; whether Holzi *should* is a product call, not an automatic parity goal.

## Recommended Order

1. [Conversation lifecycle](./01-conversation-lifecycle.md)
1b. [Conversation retention and bookmarks](./01b-conversation-retention-and-bookmarks.md)
2. [Conversation search and organization](./02-conversation-search-organization.md)
3. [Chat cancel and run state](./03-chat-cancel-and-run-state.md)
3b. [Agent runs and observability](./03b-agent-runs-and-observability.md)
4. [Retry last response](./04-chat-retry-last-response.md)
5. [Edit and regenerate](./05-chat-edit-and-regenerate.md)
6. [Streaming resilience and queued sends](./06-streaming-resilience-and-queueing.md)
7. [Chat rendering polish](./07-chat-rendering-polish.md)
8. [Tool call cards + event taxonomy](./08-tool-call-cards.md)
9. [Approvals and safety cards](./09-approval-cards.md)
10. [Reasoning and subagent cards](./10-reasoning-and-subagent-cards.md)
11. [Attachments](./11-attachments.md)
11b-a. [Sandbox spine (lifecycle + exec + isolation)](./11b-a-sandbox-spine.md)
11b-b. [Sandbox runtime remainder (read/write/git + health + UI)](./11b-sandbox-runtime.md)
12. [Workspace browser read-only](./12-workspace-browser-readonly.md)
13. [Workspace write operations and Git status](./13-workspace-write-git.md)
14. [Control center shell](./14-control-center-shell.md)
15. [Memory panel](./15-memory-panel.md)
16. [Tasks and cron panel](./16-tasks-cron-panel.md)
17. [Cline/Roo first-class channel](./17-cline-roo-first-class-channel.md)
18. [Profiles, spaces, and model routing](./18-profiles-spaces-model-routing.md) — likely supplanted by workspaces; do not implement unless a new need appears
19. [Production hardening](./19-production-hardening.md)
20. [Onboarding, diagnostics, and docs](./20-onboarding-diagnostics-docs.md)
21. [Approval granularity](./21-approval-granularity.md)
22. [UI polish — toaster, modals, drag&drop](./22-ui-polish-toaster-modals-dnd.md)
23. [Composer chips — model, reasoning, workspace](./23-composer-chips.md)
24. [Workspace Git extended](./24-workspace-git-extended.md)
25. [Multi-workspace CRUD](./25-multi-workspace-crud.md)
26. [Conversation deep-links](./26-conversation-deep-links.md)
27. [Insights and logs viewer](./27-insights-and-logs.md)

### Dependencies

The list above is a recommended order, not a strict topological sort. Each plan
states its own `Depends on:` line. The non-obvious links to remember:

- 08 establishes the SSE event envelope (`event`, `version`, `data`); 09 and 10
  add new event types but reuse the envelope.
- 11 (attachments) relies on the per-conversation scratch directory introduced
  in 01b.
- 11b introduces sandbox containers; 12 and 13 (workspace) and 16 (tasks/cron
  that execute code) all route their risky work through sandboxes from there.
- 13's "Save" means a workspace file write, not a conversation save (there is
  no such thing). Conflict detection only applies when the user opts in to
  bind-mounting the workspace volume to a host directory.
- 20 (diagnostics) consumes the run history exposed by 03b and the sandbox
  crash log exposed by 11b.

## Session Definition

Each plan is intended to be small enough for one focused implementation session.
If a plan starts to sprawl, split it before implementation. A good session ends
with:

- backend and frontend code compiling
- targeted tests added or updated
- generated API types refreshed if backend shapes changed
- a short manual verification note
- no unrelated refactors

## Cross-Repo Workflow

Most plans touch both repositories:

- backend: `/home/haex/Projekte/Holzi`
- frontend: `/home/haex/Projekte/holzi-frontend`

When a backend API changes:

1. Add or update backend tests first.
2. Implement the backend endpoint/schema.
3. Regenerate frontend API types.
4. Implement the frontend.
5. Add frontend tests for composables or high-risk UI state.

## Non-Goals

- Do not replace Holzi with upstream Hermes WebUI.
- Do not copy upstream file structure blindly.
- Do not make the web UI the source of truth for agent state.
- Do not add a large abstraction unless it is needed by at least two clients.
